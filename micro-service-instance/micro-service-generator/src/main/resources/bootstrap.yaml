server:
  port: 9999
spring:
  cloud:
    # 配置中心
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEV
  # 服务名称
  application:
    name: MICRO-GENERATOR-SERVICE
#请求处理的超时时间
ribbon:
  ReadTimeout: 120000
#请求连接的超时时间
  ConnectTimeout: 30000