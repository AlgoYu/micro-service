server:
  port: 10001
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEV
  application:
    name: MICRO-PROVIDER-SERVICE
management:
  endpoints:
    web:
      exposure:
        include: '*'